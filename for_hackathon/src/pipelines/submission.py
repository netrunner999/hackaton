async def MyAlg(
    dataset_id: str,
    num_examples: int,
    split: str,
    judge_cfg: dict,
    model_cfg: dict,
    agent_prompt_current: str,
    llm_client,
    seed: int = 42
) -> tuple[str, dict]:
# Your code here
    return improved_prompt, metadata
